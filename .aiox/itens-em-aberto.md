# Itens em Aberto

Lista leve e incremental de coisas discutidas em conversa mas ainda não formalizadas em arquivo, ou tarefas adiadas — alimentada em tempo real pela BLOCO 2-B do `CLAUDE.md`.

**Como usar:**
- Qualquer agente adiciona uma linha aqui NA HORA que algo fica "combinado só de boca" (spec, formato, decisão) ou uma tarefa é adiada.
- A BLOCO 0-K checa este arquivo (não o `.jsonl` inteiro) antes de qualquer oferta de handoff — é por isso que precisa ficar sempre atualizado.
- Remover a linha quando o item for formalizado/resolvido — este arquivo não deve virar um histórico, só o que está genuinamente em aberto agora.

**Formato de cada linha:**
`- [DATA] [agente que registrou] — [item, em 1 linha] — [arquivo/local afetado]`

---

## Itens abertos

- [10/08/2026] aiox-master — Retrofit técnico completo (formato de saída visível + checagem barata) ainda falta ser aplicado nas BLOCO 0-C, 0-N, 0-O, 0-L — hoje elas só têm a referência ao PRINCÍPIO, não o mecanismo — arquivo: `.claude/CLAUDE.md`
- [10/08/2026] aiox-master — `check-handoff-audit.js` foi testado só com 3 cenários manuais construídos, não com uso real em produção — regex de detecção de oferta de handoff pode ter falso positivo/negativo ainda não descoberto — arquivo: `.claude/hooks/check-handoff-audit.js`
- [11/08/2026] dev — INCIDENTE (reprioritizado por Felipe): página real "Produtos Perdendo Catálogo em Ads" segue renomeada pra "Prioridade - Fora de Ads" com linhas 1-2 sobrescritas — Felipe esclareceu que os dados dessa página são cópia desatualizada da página 1 ("Produtos em Ads Atualmente") de uma análise antiga, então NÃO é urgente restaurar o conteúdo exato de antes — prioridade real é terminar a análise das linhas 2-91 (Excel local `Analise Oficial.xlsx`) o mais rápido possível, pra atualizar as 4 páginas do Google Sheets de uma vez, com dado atual — arquivo/local: Planilha do Ads ML (Google Sheets)
- [13/08/2026] analyst (via *elicit, a pedido do @dev) — scripts ad-hoc de automação do Modo Navegador (Playwright/CDP) abrem aba nova (`openBackgroundPage`) mas só chamam `page.close()` no caminho feliz — em erro/timeout a aba fica aberta. Causou acúmulo real de 8 abas duplicadas da mesma planilha Google Sheets numa sessão, contribuindo pra travamento por sobrecarga de sincronização colaborativa em tempo real. Corrigir o padrão (page.close() garantido em finally, ou reaproveitar aba existente em vez de sempre abrir nova) — não é escopo do @analyst corrigir, registrado pra o @dev tratar quando conveniente — arquivo/local: scripts em `packages/karzen/.aiox-runtime/*.js` que usam `openBackgroundPage`
- [13/08/2026] dev — ainda restam ~45 arquivos não commitados em `packages/karzen/.aiox-runtime/` (scripts `teste-linha2-*`/`teste-linha3-*`/`teste-linha4-*`, `achar-catalogo-lote.js`, `colorir-lote.ps1`, `criar-paginas-sheets.js`, `processar-lote.js`, JSONs de estado, etc.) que parecem ser de um esforço anterior de processamento linha-a-linha da planilha `Analise Oficial.xlsx` — não são desta sessão (só os scripts da investigação WAP W2 4.17L foram commitados agora, ver commit `c980728`). Não commitados por não ter contexto suficiente pra saber se ainda são relevantes ou já obsoletos — precisa o Felipe decidir se comita, descarta ou se ainda estão em uso — arquivo/local: `packages/karzen/.aiox-runtime/`
- [14/08/2026] analyst (via *elicit) — nunca foi testado o pipeline contra um SKU com muitos cards (6, 20+) -- mesmo depois de corrigir rolagem exaustiva e ID Family, não há confirmação real de que funciona nessa escala até encontrar um produto assim de verdade -- arquivo: `packages/karzen/.aiox-runtime/pipeline-pausados-campanha-completo.js`
- [14/08/2026] dev — RESOLVIDO (commit `1275b11`): as 2 falhas de checagem via Alterar em MLBs revelados dentro de "ID Family" (SKU `PROSB-3000`) tinham causa confirmada — cada header de grupo "ID Family" mantém seu próprio botão `aria-label="Ações secundárias"` mesmo depois de expandido, deslocando o índice posicional de todos os MLBs seguintes. Corrigido via `construirOrdemBotoes()`, que escaneia todas as linhas `#\d+` (não só `#\d{7,11}`) e insere `null` nos índices dos headers de grupo, preservando o offset correto. Verificado ponta a ponta contra PROSB-3000 (6/6 MLBs batendo com o mapeamento real de índices) e contra MCT-32MM-BIV sem ID Family (4/4 batendo com o gabarito) — nada quebrou. O caso do MLB `#5267746592` (preço "239,90" sem bater) era na verdade um card legítimo de "ML inativou o anúncio" (padrão já conhecido "É igual a outro anúncio"), não um bug.
- [14/08/2026] dev (recomendado pelo @analyst na validação da correção acima) — a convenção usada pra distinguir cabeçalho de grupo "ID Family" de MLB real (comprimento do número: `/^#\d{7,11}$/` pra MLB real, qualquer outro tamanho é ignorado) é baseada em 1 caso real só (`PROSB-3000`, Product ID de 16 dígitos) -- não é uma invariante confirmada do Mercado Livre. Foi adicionada uma camada de defesa (checar preço em FAIXA como sinal secundário, ver comentário "Item 2" em `analisarSku`), mas isso mitiga, não prova -- só mais produtos reais com "ID Family" confirmando o padrão de dígitos resolveriam isso de verdade -- arquivo: `packages/karzen/.aiox-runtime/pipeline-pausados-campanha-completo.js`
- [14/08/2026] dev (RESOLVIDO -- checklist de 5 itens do @analyst sobre a lacuna do casamento de preço, commit a registrar) — corrigida a lacuna estrutural que permitia entradas de "Outras opções de venda" (referência ao próprio anúncio irmão, ex: "Clássico e Frete grátis") virarem `statusCatalogo` por coincidência de preço com outro MLB -- essas entradas agora são rejeitadas via `FRASES_NAO_STATUS` (viram `status:null`, ficam inelegíveis pro casamento). Adicionado também autocheck de ambiguidade residual (avisa, não trava, se 2+ opções com status real compartilharem o mesmo preço escolhido) -- cobre tanto colisão por `precoBase` quanto por `precoPromo`. Testado com dados fabricados (rejeição + ambiguidade por base + ambiguidade por promo) e reprocessado contra os 6 SKUs reais conhecidos, sem regressão -- arquivo: `packages/karzen/.aiox-runtime/pipeline-pausados-campanha-completo.js`
- [14/08/2026] dev (recomendado pelo @analyst, item 4 do checklist -- NÃO implementado, só registrado) — a extração de `condicao` usa regex fixa `/\b(Clássico|Premium)\b/` (2 ocorrências no arquivo) -- assume só 2 faixas de exposição possíveis no Mercado Livre. Nunca visto um produto com 3+ condições nesta sessão, então implementar suporte a isso seria especulativo sem caso real. Resolver só se/quando aparecer um produto real com 3+ condições -- arquivo: `packages/karzen/.aiox-runtime/pipeline-pausados-campanha-completo.js`
